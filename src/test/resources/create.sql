CREATE TABLE HOTEL (ID VARCHAR(255) NOT NULL, NAME VARCHAR(255), PRIMARY KEY (ID))
CREATE TABLE ROOM (ID VARCHAR(255) NOT NULL, NAME VARCHAR(255), HOTEL_ID VARCHAR(255), PRIMARY KEY (ID))
CREATE TABLE PC (ID VARCHAR(255) NOT NULL, NAME VARCHAR(255), PRIMARY KEY (ID))
CREATE TABLE PARTS (ID VARCHAR(255) NOT NULL, NAME VARCHAR(255), PRIMARY KEY (ID))
CREATE TABLE PC_PARTS (pcs_ID VARCHAR(255) NOT NULL, parts_ID VARCHAR(255) NOT NULL, PRIMARY KEY (pcs_ID, parts_ID))
ALTER TABLE HOTEL ADD CONSTRAINT UNQ_HOTEL_0 UNIQUE (name)
ALTER TABLE ROOM ADD CONSTRAINT UNQ_ROOM_0 UNIQUE (id, hotel_id)
ALTER TABLE ROOM ADD CONSTRAINT FK_ROOM_HOTEL_ID FOREIGN KEY (HOTEL_ID) REFERENCES HOTEL (ID)
ALTER TABLE PC_PARTS ADD CONSTRAINT FK_PC_PARTS_pcs_ID FOREIGN KEY (pcs_ID) REFERENCES PC (ID)
ALTER TABLE PC_PARTS ADD CONSTRAINT PC_PARTS_parts_ID FOREIGN KEY (parts_ID) REFERENCES PARTS (ID)
